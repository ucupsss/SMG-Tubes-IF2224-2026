# This repository already contains LF, CRLF, and mixed-EOL files.
# Disable automatic EOL conversion so Windows Git and WSL Git do not rewrite
# unrelated files during `git add`.
* -text

# Binary artifacts must never be treated as text.
*.pdf binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.exe binary
*.jar binary
