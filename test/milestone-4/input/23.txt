program RuntimeArrayBounds;

type
    Index == 1..3;
    Numbers == array[Index] of integer;

var
    nums: Numbers;
    i: integer;

begin
    readln(i);
    nums[i] := 42;
    writeln('unreachable');
end.
