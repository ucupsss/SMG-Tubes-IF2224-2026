program BranchLoop;

var
    x, y: integer;

begin
    if x > 0 then y := 1 else y := 0;
    while y < 10 do
    begin
        y := y + 1
    end;
    repeat
        y := y - 1
    until y == 0;
end.
